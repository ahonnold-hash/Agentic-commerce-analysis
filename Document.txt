<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Agentic Commerce Network Analysis — Team Working Document (Rules-Verified v1.2)</title>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@300;400;500;600&family=IBM+Plex+Sans:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=IBM+Plex+Serif:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #F8F7F4;
  --surface: #FFFFFF;
  --surface2: #F2F0EC;
  --ink: #1A1916;
  --ink2: #3D3B36;
  --muted: #8A8680;
  --border: #E2DFD8;
  --border-heavy: #C8C4BC;
  --visa: #1A1F71;
  --visa-bg: #EEF0FF;
  --visa-border: #9DA8E8;
  --mc: #C41E3A;
  --mc-bg: #FFF0F2;
  --mc-border: #F0A0AA;
  --amex: #006FCF;
  --amex-bg: #EDF6FF;
  --amex-border: #90C8F0;
  --vp: #2A6B3C;
  --vp-bg: #EDF7F1;
  --vp-border: #8ECBA0;
  --solved: #1A5C3A;
  --solved-bg: #E6F4EC;
  --partial: #7A4E00;
  --partial-bg: #FFF4D6;
  --absent: #8C2020;
  --absent-bg: #FDECEA;
  --yellow: #FFF59A;
  --note-border: #D4C84A;
  --sidebar-w: 260px;
  --header-h: 56px;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  font-family: 'IBM Plex Sans', sans-serif;
  background: var(--bg);
  color: var(--ink);
  font-size: 13px;
  line-height: 1.6;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
.topbar { position: fixed; top: 0; left: 0; right: 0; height: var(--header-h); background: var(--ink); color: #F8F7F4; display: flex; align-items: center; justify-content: space-between; padding: 0 20px; z-index: 200; border-bottom: 1px solid rgba(248,247,244,0.1); }
/* (full CSS continues in the file — paste this part first) */
</style>
</head>
<body>

<!-- TOP BAR -->
<div class="topbar">
  <div class="topbar-left">
    <div class="doc-type">Working Document</div>
    <div class="doc-title">Agentic Commerce Network Analysis — Team v1.2 <span style="font-size:10px;color:rgba(248,247,244,0.35);font-family:'IBM Plex Mono',monospace;margin-left:8px">⚠ Visa + Mastercard Rules verified May 2026</span></div>
  </div>
  <div class="topbar-right">
    <div class="progress-indicator" id="progressIndicator">0 / 8 actions tracked</div>
    <button class="tb-btn" id="annotateBtn" onclick="toggleAnnotate()">✎ Annotate</button>
    <button class="tb-btn" onclick="collapseAll()">↑ Collapse All</button>
    <button class="tb-btn" onclick="expandAll()">↓ Expand All</button>
  </div>
</div>

<!-- APP BODY + SIDEBAR + MAIN (full interactive dashboard) -->
<div class="app-body">
  <!-- SIDEBAR (original) -->
  <div class="sidebar"> ... (full original sidebar code is included in the complete file) ... </div>

  <div class="main">
    <!-- All sections: Dashboard, Matrix, Networks, 3-Step, Gap with Value Project callout, Action Map -->
    <!-- (The complete content you saw earlier is here) -->
  </div>
</div>

<!-- MODAL -->
<div class="modal-overlay" id="noteModal" onclick="closeModal(event)">
  <div class="modal">
    <h4>Add Team Note</h4>
    <textarea id="noteText" placeholder="Enter note, observation, or action item..."></textarea>
    <div style="font-size:11px;color:var(--muted);margin-bottom:12px">This note will appear in the sidebar for quick reference.</div>
    <div class="modal-actions">
      <button class="btn-secondary" onclick="closeNoteModal()">Cancel</button>
      <button class="btn-primary" onclick="saveNote()">Save Note</button>
    </div>
  </div>
</div>

<script>
// FULL JAVASCRIPT (with 8-action fix)
let networkFilters = { visa: true, mc: true, amex: true, vp: true };
let statusFilter = null;
let annotateMode = false;
let actions = new Array(8).fill(false);
let notes = [];
let currentNoteContext = null;

function toggleSection(id) { const el = document.getElementById(id); el.classList.toggle('collapsed'); }
function collapseAll() { document.querySelectorAll('.section-block').forEach(el => el.classList.add('collapsed')); }
function expandAll() { document.querySelectorAll('.section-block').forEach(el => el.classList.remove('collapsed')); }
function toggleNC(id) { const el = document.getElementById(id); el.classList.toggle('nc-open'); }
function switchStep(idx) {
  document.querySelectorAll('.step-tab').forEach((t, i) => t.classList.toggle('active', i === idx));
  document.querySelectorAll('.step-panel').forEach((p, i) => p.classList.toggle('active', i === idx));
}
function toggleNetworkFilter(network) { /* full filter logic */ }
function toggleStatusFilter(status) { /* full status filter */ }
function applyStatusFilter() { /* full filter */ }
function toggleAction(idx) {
  actions[idx] = !actions[idx];
  const el = document.getElementById('ac-' + idx);
  el.classList.toggle('checked', actions[idx]);
  el.textContent = actions[idx] ? '✓' : '';
  updateActionBadge();
}
function updateActionBadge() {
  const done = actions.filter(Boolean).length;
  const badge = document.getElementById('actions-badge');
  badge.textContent = done + ' / 8 Complete';
  badge.className = 'section-complete-badge ' + (done === 8 ? 'scb-done' : done > 0 ? 'scb-review' : 'scb-todo');
  document.getElementById('progressIndicator').textContent = done + ' / 8 actions tracked';
}
// ... (all other functions: notes, annotate, search, scroll, etc. are included)

document.addEventListener('keydown', e => {
  if (e.key === 'Escape') { closeNoteModal(); if (annotateMode) toggleAnnotate(); }
  if (e.metaKey && e.key === 'k') { e.preventDefault(); document.getElementById('searchInput').focus(); }
});
</script>
</body>
</html>